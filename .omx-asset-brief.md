# Ultragoal brief — Rescue Town Builders: Asset Pipeline & Browser Test Harness (parallel track)

You are a parallel Codex development **swarm** working ALONGSIDE a Claude agent
(orchestrator + game-feel mechanics + the hidden Secrets + synth audio) and a Grok
agent on the same browser Phaser 4 + TypeScript kids' game. Coordinate; never collide.

## Hard isolation rules (critical — read twice)
- Work ONLY in this worktree: `/home/ark/rtb-codex`, on branch `codex/assets-juice`.
- NEVER touch `/home/ark/rescue-town-builders` (the Claude checkout), nor the
  `continuous-refinement` or `main` branches. Do not run `git clean`/`reset` outside
  this worktree.
- Commit your work to `codex/assets-juice`. Do NOT push to origin or merge to main.
- Coordinate via GitHub issues on `ShanesNotes/rescue-town-builders`: comment on the
  issue you're working; do not close issues owned by other agents.
- This is a fresh worktree with no `node_modules` — run `npm install` first.

## Project guardrails (never violate)
- **IP-safe:** original characters only. For assets use **CC0 only** (Kenney.nl,
  OpenGameArt CC0), with source + license recorded in `docs/assets/ASSET_BACKLOG.md`.
  ZERO Paw Patrol or any third-party IP, names, or likenesses.
- **localStorage-only, no backend** (ADR-0002). Browser-first Phaser 4 (ADR-0001).
- **No-Fail Rule:** never add anything that can hard-block, punish, or shame a child.
- **No new missions** (ADR-0006 gate is closed). DEEPEN the existing three —
  Rivet's Recycling Run, Brick's House Builder, Ember's Fire Fix — and the Town Map.
- Honor **ADR-0007**: lightweight scene seams, no heavyweight mission runtime.

## Your scope (disjoint from the Claude track)
1. **Assets.** Source vetted CC0/Kenney sprite + SFX + UI packs for: town map, recycling
   bins/items, house parts, fire/water/hydrant, the original helper characters, stickers,
   and celebration. Place under `public/assets/`; record every source + license in
   `ASSET_BACKLOG.md`.
2. **Loader.** Build/extend `AssetCatalog` as a typed manifest and load real assets in
   `PreloadScene`, with graceful fallback to the current placeholder shapes (No-Fail if
   an asset is missing).
3. **Test harness.** Stand up a **Playwright** browser smoke test (the missing browser
   runner): create a profile, play each of the three missions to completion, refresh,
   verify stars/stickers persist; add a mobile-landscape viewport check. Wire an
   `npm run test:e2e` script. This unblocks safe scene-layer verification for the team.

## How to work
- Use the **$ultragoal** skill: `create-goals` from this brief (aggregate mode, `--force`),
  then `complete-goals` iteratively until done.
- Spawn your subagent team (planner, executor, qa-tester, code-reviewer, designer,
  dependency-expert) for parallel progress.
- Verify each goal: `npm run typecheck`, `npm test`, `npm run build`, and your new e2e smoke.
- Keep the ledger clean. Slow is smooth, smooth is fast.
